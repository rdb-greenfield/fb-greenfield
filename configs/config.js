// Launch app - (separate tabs)
// npm run react-dev
// npm run server-dev
// npm start

// MYSQL workflow
// - start mysql locally
// mysql.server start

// Create the database locally
// - mysql -u root < schema.sql (from root directory)

// To access mysql prompt from terminal - run
// - sudo mysql, enter password
// - use greenfield;

// Create the admin user account locally, run these commands from mysql prompt
// - create user ‘admin’@‘localhost’ identified by ‘admin123’;
// - grant all privileges on greenfield.* to ‘admin’@‘localhost’;
